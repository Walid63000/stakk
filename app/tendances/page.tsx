import { TrendsView } from "@/components/TrendsView";

// Écran Tendances : graphes 7/30 jours — récup en points colorés par
// zone × strain en ligne, puis sommeil, HRV, FC en sparklines.

export default function TendancesPage() {
  return <TrendsView />;
}
